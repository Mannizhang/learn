#import shutil
#shutil.copyfile(‘listfile.py’, ‘d:/test.py’) #复制文件
#shutil.copystat(src,dst) #拷贝文件，连同文件的stat一起拷贝
#shutil.copytree(src,dst) #拷贝目录，拷贝之前dst必须不存在
#shutil.copy2(src,dst)#拷贝文件，连同文件的stat信息一起拷贝，不能拷贝目录
#不会用